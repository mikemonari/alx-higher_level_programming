#!/usr/bin/python3
if __name == "__main__":
    def replace_in_list(my_list, idx, element):
        if idx >= len(my_list) and idx < len(my_list):
            return my_list
        mylist[idx] = element
        return my_list
